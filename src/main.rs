// is_even
fn is_even(n: i32) -> bool {
    if n % 2 == 0 { true } else { false }
}

fn main() {
    println!("{}", is_even(4)); //output: true
    println!("{}", is_even(7)); //output: false
}


fn is_prime(n: i32) -> bool }
   if n < 2 {
      return false;
   }

   for i in 2..n { 
       if n % i == 0 {
          return false;
       }

   true
   }
  
fn main() { 
println!("{}", is_prime(7)); // Prints: true
}
